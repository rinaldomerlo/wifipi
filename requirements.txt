flask>=2.0.0
gunicorn>=20.1.0
# video_stream_simulator only: connection pooling. A fresh TCP connection per segment
# pays a handshake plus TCP slow start every time, which at 200 ms RTT caps measured
# throughput near 1.3-1.8 Mbps regardless of the real link and makes the upper ladder
# rungs unreachable -- the client would measure its own connection setup, not the WiFi.
requests>=2.25.0
